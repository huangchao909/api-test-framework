# 测试环境 (dev / test / staging / prod)
TEST_ENV=test

# API 基础地址（留空则根据 ENV 自动选择）
BASE_URL=https://jsonplaceholder.typicode.com

# 请求配置
REQUEST_TIMEOUT=15
MAX_RETRIES=3

# 认证（按需填写）
AUTH_TOKEN=
AUTH_USERNAME=
AUTH_PASSWORD=
